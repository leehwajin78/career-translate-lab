# 한끗프로젝트 — 제품 요구사항 정의서 (PRD)

| 항목 | 내용 |
| :--- | :--- |
| **문서 버전** | v3.0 (출시 기준 · Phase 1 baseline) |
| **최종 작성일** | 2026-06-13 |
| **서비스명** | 한끗프로젝트 |
| **운영 주체** | 꿈몰다 · 이화진 대표 |
| **기준 코드베이스** | `career-translate-lab` (Vite + React 18 + TypeScript + Zustand + TailwindCSS) |
| **기준선 정의** | 첫 유료 출시(Phase 1 완료) 상태를 정본으로 한다. 미구현 항목은 상태 배지로 구분한다. |

### 상태 배지 범례

| 배지 | 의미 |
| :--- | :--- |
| **[현행]** | 현재 코드베이스에 이미 구현됨 |
| **[P1]** | Phase 1 목표 — 첫 유료 출시 전 구현 (작업 명세 T1~T10) |
| **[P1.5]** | 출시 직후 보강 (이메일·음성 복원) |
| **[P2]** / **[P3]** | 이후 로드맵 (AI API · 결제 등) |

---

## 1. 제품 비전 및 핵심 가치

### 1-1. 한 줄 정의

> **"50~60대 시니어 경력 전문가의 30년 경력을 시장이 선택하는 자산으로 번역하는 프리미엄 1:1 경력 자산화 서비스"**

### 1-2. 핵심 메시지

- **Hero Copy**: *"30년을 일했는데, 나를 소개하는 한 문장이 없습니다."*
- **브랜드 철학**: *"경력은 충분합니다. 부족한 건 번역입니다. 시장이 선택하는 자산으로 만듭니다."*
- **포지셔닝**: 강사 양성 과정이 아닌, 경력 있는 전문가의 경험을 시장 통용 언어로 번역하여 프로필·강의안·제안서 등의 실전 산출물로 만드는 **Done-for-you 하이브리드 코칭 서비스**

### 1-3. 운영 전제

- **1인 운영 기준.** 모든 기능·정원·워크플로는 1인(코치) 운영 부하를 전제로 설계한다.
- **AI 초안 → 코치 검수 → 고객 전달** 원칙. 고객에게 노출되는 산출물은 코치 확정(finalized)을 거친다.
- **코호트 정원 제한.** 동시 진행 인원을 통제해 품질과 운영 부하를 관리한다(§9 운영 모델).

### 1-4. 타깃 고객

| 구분 | 설명 |
| :--- | :--- |
| **연령** | 50~60대 (핵심 타깃) |
| **직업** | 퇴직 예정 임원, 교수, 기술 전문가, 중소기업 대표 등 |
| **핵심 페인포인트** | 직함을 벗기면 자기를 설명할 언어가 없음. 경험은 풍부하나 제안서·강의안·프로필 등 시장 진입 도구가 없음 |
| **고객 특성** | "한 번에 제대로" 세대 — 선택 부담을 회피하고 즉시 효용을 중시. UX·카피·산출물 모두 이 원칙으로 판단 |

### 1-5. 해결하는 문제 (4대 페인포인트)

1. **직함 의존** — 직함 없이는 자기 소개가 불가능한 상태
2. **메시지 부재** — 경험이 시장이 이해할 메시지로 전환되지 않은 상태
3. **자산 산재** — 프로필·소개문·제안서·강의안이 제각각으로 일관된 신뢰 부재
4. **실행 부재** — 혼자 만들면 멈추고, 외주를 맡기면 맥락이 빠지는 구조적 한계

---

## 2. 서비스 상품 체계

### 2-1. 4단계 패키지 구조

> **가격 노출 정책:** 사이트 외부에는 진단·빌드만 금액을 노출하고, 론칭·파트너는 **"별도 문의"**로 표기하여 상담으로 유도한다. 아래 내부 금액은 운영 기준이다.

| STEP | 상품명 | 내부 기준가 | 사이트 노출 | 기간 | 핵심 포지셔닝 |
|:---:|:---|:---:|:---:|:---:|:---|
| **1** | **한끗 진단** | 50만원 | 50만원 | 1주 | 내 경력의 시장 가치를 진단합니다 |
| **2** | **한끗 빌드** | 350만원 | 350만원 | 6주 | 강의안·프로필·제안서를 완성합니다 |
| **3** | **한끗 론칭** | 700만원 | 별도 문의 | 3개월 | 실제 무대와 수익 기회에 접근하도록 지원합니다 |
| **후속** | **한끗 파트너** | 월 100만원 | 별도 문의 | 월 단위 | 매월 점검하고, 다음 기회를 설계합니다 |

### 2-2. 상품별 포함 내역

**STEP 1. 한끗 진단 (50만원 · 1주)** — 핵심 메시지(원라이너) 1문장(코치 확정), 강점 명제문(간략 확정), 브랜드 요소 6종 미리보기, 경력 자산 진단 리포트(A4 3~5장), 자산화 로드맵 1장, 30분 해석 미팅

**STEP 2. 한끗 빌드 (350만원 · 6주)** — 브랜드 전략 브리프 / 확정 원라이너·브랜드 매니페스토·핵심 카피 3종 / 전문가 프로필 1p, 대표 강의안(60분), B2B 제안서 템플릿, 채널 전략 가이드, 소개 멘트 3종

**STEP 3. 한끗 론칭 (700만원 · 3개월)** — 빌드 전체 + 강의 리허설·피드백 2회 + 온라인 프로필 페이지 + 제안처 발굴·소개 + 3개월 론칭 코칭(월 2회)

**후속. 한끗 파트너 (월 100만원)** — 월 2회 코칭 + 콘텐츠 리뷰·업데이트 + 신규 기회 탐색·소개 + 분기 브랜드 점검 리포트

### 2-3. 첫 코호트 운영 제약

- **입력 방식:** 첫 코호트는 **텍스트 전용**. 음성 입력은 설계상 존재하나 비활성(§5-2, §7-1).
- **동시 인원:** 파트너 동시 5~8명 제한. 빌드·론칭은 검수 부하를 고려해 코치 판단으로 신규 유입 조절.

---

## 3. 사용자 여정 (User Journey)

### 3-1. 전체 서비스 플로우

```
[방문자]
  ├─ 메인 랜딩(/)
  │   ├─ "경력 가치 무료 진단받기" → 무료 7문항 자가 진단(/diagnosis)
  │   ├─ "30분 무료 상담 신청하기" → 상담 신청(/consultation)
  │   ├─ 서비스 소개(/service)
  │   └─ 상품별 신청 → 신청서(/apply/*)
  │
  ├─ 무료 자가 진단
  │   [개인정보 동의] → 이메일·이름 → 7문항 → 분석 로딩 → 진단 리포트 → 상담/신청 유도
  │
  ├─ 유료 신청
  │   [개인정보 동의] → 상품 확인 → 신청서 → 완료(/apply/thank-you) → leads 저장
  │
  ├─ 유료 코칭 (멤버 전용)
  │   어드민 계정 발급 → 로그인(/login) → 대시보드(/coaching)
  │   → 42문항(/coaching/questions) → 리뷰·제출(/coaching/review)
  │   → 분석(/coaching/analyzing) → [코치 검수·확정] → 리포트(/coaching/report)
  │
  └─ 관리자 운영 [인증 필요]
      어드민 콘솔(/admin): 리드 CRM + 계정 발급 + 답변 조회
      워크스페이스(/coaching/workspace/:memberId): 검수 → finalized 확정
```

### 3-2. 전환 퍼널 + 검수 게이트

```
방문 → 무료 진단 → 결과 확인 → 상담/유료 신청 → 계약 → 계정 발급
  → 42문항 작성 → 제출(submitted) → 코치 검수 → 확정(finalized) → 리포트 노출
```

> **변경점 [P1]:** 코칭 리포트는 코치가 `finalized` 확정하기 전까지 고객에게 노출되지 않는다(검수 게이트, §8-3). 모든 개인정보 수집 지점에 **동의 단계**가 선행한다(§8-4).

---

## 4. 페이지 구조 및 라우팅

| 경로 | 페이지 | 접근 제한 | 상태 |
| :--- | :--- | :---: | :---: |
| `/` | 메인 랜딩 (10섹션) | 공개 | [현행] |
| `/service` | 서비스 상세 소개 | 공개 | [현행] |
| `/diagnosis` | 무료 7문항 자가 진단 | 공개 | [현행] |
| `/consultation` | 30분 무료 상담 신청 | 공개 | [현행] |
| `/apply/diagnosis · build · launch · partner` | 상품별 신청 | 공개 | [현행] |
| `/apply/thank-you` | 신청 접수 완료 | 공개 | [현행] |
| `/privacy` | 개인정보 처리방침 | 공개 | **[P1]** |
| `/login` | 멤버 로그인 | 공개 | [현행]→[P1] Auth 교체 |
| `/coaching` | 코칭 진행률 대시보드 | 로그인 | [현행] |
| `/coaching/questions` | 42문항 작성 | 로그인 | [현행] |
| `/coaching/review` | 답변 리뷰·제출 | 로그인 | [현행] |
| `/coaching/analyzing` | 분석 진행 화면 | 로그인 | [현행] |
| `/coaching/report` | 코칭 리포트 (finalized만) | 로그인 | [현행]→[P1] 게이트 |
| `/coaching/workspace/:memberId` | 코치 워크스페이스 | **인증 필요** | [현행]→[P1] 인증 |
| `/admin` | 관리자 콘솔 (리드+멤버 2탭) | **인증 필요** | [현행]→[P1] 인증 |
| `*` | 404 | 공개 | [현행] |

> **제거:** `/result`(레거시 진단 결과)는 미사용으로 라우트에서 제외한다.

---

## 5. 핵심 기능 상세

### 5-1. 무료 자가 진단 시스템 (7문항) [현행]

| Q# | 영역 | 질문 핵심 |
|:---:|:---|:---|
| 1 | 정체성 | 직함 없이 나를 소개한다면? |
| 2 | 핵심 가치 | 가장 자랑스러웠던 순간 3가지 |
| 3 | 강점 | 자연스럽게 잘 되는 것 3가지 |
| 4 | 권위 | 사람들이 조언 구하러 오는 분야 |
| 5 | 타깃 | 돕고 싶은 사람 (마음 상태 기준) |
| 6 | 차별화 | 나만의 차별점 |
| 7 | 메시지 | 전하고 싶은 핵심 메시지 한 문장 |
| 보너스 Q8 | 체크리스트 | 지금 바로 보낼 수 있는 자료 (복수) |

- **플로우:** 개인정보 동의 → 이메일·이름·경력연수 → 7문항 순차 입력 → 분석 로딩 → 결과 리포트 → CTA
- **분석 엔진:** 로컬 알고리즘 `analyzeFree()` — 텍스트 길이·키워드 규칙·영역 가중치. 종합 점수(100) + 5영역 점수
- **유형 분류 (4종):** 탐색형 🔍 / 준비형 📦 / 전환형 🔄 / 실행형 🚀
- **저장 [P1]:** 결과를 `free_diagnostics` 테이블에 저장(동의 시각 포함)

### 5-2. 유료 코칭 시스템 (42문항)

**4대 파트** — PART1 살아온 삶(Q01–10) / PART2 보유 자산(Q11–22) / PART3 원하는 것(Q23–32) / PART4 말하는 법(Q33–42)

- **입력 모드:**
  - ✏️ **글로 쓰기** [현행] — 주관식 텍스트, 실시간 글자수
  - 🎤 **말로 녹음** [현행·설계 보존] — Web Audio API 기반. **첫 출시 비활성**(`VITE_VOICE_ENABLED=false`). 음성 클라우드 저장(B2) 해결 후 [P1.5]에 재활성화. 코드는 삭제하지 않고 flag로만 숨김
- **진행률·네비게이션:** `completedCount/42` 시각화, QuestionNav 드로어 [현행]
- **저장 [P1]:** Zustand는 UI 캐시, 영속은 `coaching_sessions`·`answers`(question_id upsert). 디바운스 자동 저장. **다른 기기·브라우저에서 이어쓰기 가능**
- **유형 분류 (4종):** 직함 의존형 / 경험 나열형 / 숨은 전문성형 / 시장 진입 준비형
- **상태 머신:** `in-progress → submitted → analyzing → analyzed → finalized`

### 5-3. 관리자 콘솔 [현행 + [P1] 인증]

> **[P1] 접근 통제:** AdminGate를 Supabase Auth 기반으로 교체. `app_metadata.role='admin'`이 아닌 세션은 `/admin`·워크스페이스 진입 차단(§8-1).

- **Tab 1 — 상담 리드:** 상품별 필터, 상태(대기중→상담중→완료→보류), 7문항 답변·영역 점수·추천 패키지 상세, 운영 메모 인라인 자동 저장
- **Tab 2 — 코칭 회원:** 계정 발급, 카카오톡/SMS 안내문 원클릭 복사, 42문항 진행률 추적, 답변 조회, 회원 삭제
  - **[P1] 계정 발급 변경:** Edge Function `create-member`가 Supabase Auth 사용자 생성 + `members` insert. **비밀번호 평문 저장 폐지**(§8-2)
- **알림 시스템 [현행]:** 제출 시 Web Audio 차임벨 + Notification API 데스크톱 푸시 + BroadcastChannel 멀티탭 동기화

### 5-4. 코칭 워크스페이스 [현행 + [P1] 게이트]

- 42문항 답변 파트별 조회, 문항별 코치 메모, AI 초안(AIDraft) 저장, 최종 프로필(FinalProfile) 확정
- **[P1] 검수 게이트:** 확정 버튼이 `status='finalized'` + `finalized_at`·`review_logs` 기록. 확정 전에는 고객 리포트에 `final_profile` 비노출(§8-3)

### 5-5. 상담·신청 [현행 + [P1] 동의]

- **30분 무료 상담(/consultation):** 이름·연락처·분야·경력·고민 + 목적(복수)·결과물(복수)·상담방식. `leads` 저장
- **유료 신청(/apply/*):** 4상품 독립 신청서, 결제 없이 리드 수집. 완료 시 `/apply/thank-you`
- **[P1] 동의:** 모든 수집 지점에 개인정보 동의 체크 + 시각 저장. 미동의 시 제출 차단

---

## 6. 최종 산출물 (Deliverables)

| # | 산출물 | 활용처 |
|:---:|:---|:---|
| D01 | 한 줄 포지셔닝(원라이너) | 명함, 이메일 서명, 소개 |
| D02 | 전문가 프로필 1페이지 | 미팅, 제안, 프로필 페이지 |
| D03 | 대표 강의안 (60분) | 기업 특강, 세미나, IR |
| D04 | B2B 제안서 템플릿 | 기업 대상 제안 |
| D05 | 채널 전략 가이드 | SNS, 칼럼, 콘텐츠 |
| D06 | 소개 멘트 30초·60초 | 네트워킹, 미팅 |

---

## 7. 기술 아키텍처

### 7-1. 기술 스택

| 영역 | 기술 | 상태 |
| :--- | :--- | :---: |
| 빌드/프레임워크 | Vite · React 18 · TypeScript | [현행] |
| 라우팅 | React Router DOM v6 | [현행] |
| 스타일링 | Tailwind CSS 3 · shadcn/ui (Radix) | [현행] |
| 상태관리 | Zustand — **UI 캐시 역할로 재정의** | [현행]→[P1] |
| 폼 처리 | React Hook Form · Zod | [현행] |
| 폰트 | SCDream · Noto Serif KR · Noto Sans KR | [현행] |
| **백엔드 (DB)** | **Supabase Postgres** | **[P1]** |
| **인증** | **Supabase Auth** | **[P1]** |
| **파일 저장** | **Supabase Storage** (음성용, B2 시) | **[P1.5]** |
| **서버 로직** | **Supabase Edge Functions** (create-member, send-email) | **[P1]/[P1.5]** |
| AI 분석 | 로컬 룰(`analyzeFree`, `coachingAI`) → LLM API | [현행]→[P2] |
| 배포 | Netlify | [현행] |

> **아키텍처 전환 [P1]:** 기존 "서버리스·localStorage 단독"에서 **Supabase 백엔드**로 전환. localStorage는 더 이상 영속 신뢰원이 아니다. Zustand는 화면 상태 캐시로만 사용하고, 영속은 Supabase가 담당한다.

### 7-2. 데이터 저장소 (store → 테이블 매핑)

| 기존 store | → 저장소 [P1] | 비고 |
| :--- | :--- | :--- |
| `leadsStore` | `leads` | 상담·유료신청 통합 |
| `freeDiagnosticStore`(결과) | `free_diagnostics` | 무료 7문항 결과 분리 |
| `authStore.members` | `members` + `auth.users` | 비번은 Auth가 보관 |
| `coachingStore.sessions` | `coaching_sessions` + `answers` | 음성 컬럼 nullable, 첫 출시 미사용 |
| `notificationStore` | 클라이언트 유지(또는 `notifications`) | Phase 1 단순화 |
| `diagnosticStore`(레거시) | — | 폐기 |

### 7-3. 주요 데이터 모델 [P1]

```typescript
// 회원 — auth.users 와 1:1, 평문 password 필드 없음
interface Member {
  id: string;            // = auth.users.id
  name: string;
  email: string;
  productKey: "diagnosis" | "build" | "launch" | "partner";
  createdAt: string;
}

// 코칭 세션
interface CoachingSession {
  id: string;
  memberId: string;
  status: "in-progress" | "submitted" | "analyzing" | "analyzed" | "finalized";
  currentQuestion: number;
  lastSavedAt: string;
  submittedAt?: string;
  finalizedAt?: string;
  aiDraft?: AIDraft;        // 코치 검수용 초안
  finalProfile?: FinalProfile; // 고객 노출 대상 (finalized 시)
  coachNotes?: Record<number, string>;
}

// 문항 답변 (session_id, question_id 유니크)
interface Answer {
  sessionId: string;
  questionId: number;       // 1..42
  text: string;
  voiceUrl?: string;        // 첫 출시 null
  voiceDuration?: number;
  updatedAt: string;
}

// 리드
interface Lead {
  id: string;
  name: string; phone?: string; email?: string;
  field?: string; career?: string; challenge?: string;
  purposes: string[]; outcomes: string[]; channel?: string;
  source: string;           // consultation | apply/build ...
  recommendedPackage?: string;
  memo: string;
  status: "대기중" | "상담중" | "완료" | "보류";
  createdAt: string;
}
```

스키마 DDL 전문과 RLS 정책은 **부속 문서 「Phase 1 작업 명세」 §2** 참조.

### 7-4. 컴포넌트·서버 구조 변경

- 신규: `src/lib/supabase.ts`(클라이언트), Edge Function `create-member`[P1] · `send-email`[P1.5]
- 변경: `AdminGate`(인증 게이트), `authStore`(평문 password 제거), 각 store의 영속 계층을 Supabase로 교체
- 보존: `VoiceRecordMode`·`VoicePlayer`는 유지하되 `VITE_VOICE_ENABLED`로 조건부 렌더

---

## 8. 보안·개인정보 모델 [P1] (신규)

### 8-1. 인증

- **어드민:** Supabase Auth 계정 + `app_metadata.role='admin'`. `is_admin()` 함수로 판정. 비로그인/비어드민은 `/admin`·워크스페이스 차단
- **멤버:** 어드민이 발급한 Auth 계정으로 로그인. 본인 세션·답변만 접근

### 8-2. 비밀번호

- 멤버 계정 발급을 **Supabase Auth에 위임** → 평문 비밀번호가 어떤 테이블·로그에도 존재하지 않음. 해싱은 Auth가 처리

### 8-3. 검수 게이트 (RLS 강제)

- 멤버는 `status='finalized'`인 `final_profile`만 조회 가능(`my_finalized_report` 뷰 + RLS). **애플리케이션 로직 실수로도 미검수 리포트가 노출되지 않음**
- "AI 초안 → 코치 검수 → 전달" 원칙을 권한 수준에서 보장

### 8-4. 개인정보

- **수집 지점:** 무료 진단·상담·신청 — 이름·연락처·경력·(추후)음성
- **동의:** 각 지점에 동의 체크 + 동의 시각 저장. 미동의 시 제출 차단
- **처리방침(/privacy):** 수집 항목·목적·보관기간·파기·제3자 제공·문의처 명시. *문구의 법적 검토는 외부 전문가 확인을 전제로 한다*
- **RLS:** 전 테이블 enable. 리드·답변은 비로그인 조회 불가, 익명 insert만 허용(폼 제출)

---

## 9. 운영 모델 (신규)

- **1인 운영 전제.** 코치 1인이 리드 검토 → 계약 → 계정 발급 → 42문항 검수 → 리포트 확정의 루프를 운영
- **검수 워크플로:** 멤버 제출(submitted) → 알림 수신 → 워크스페이스에서 답변·메모 검토 → AIDraft 검토·수정 → FinalProfile 확정(finalized) → 고객 노출
- **코호트 정원:** 파트너 동시 5~8명. 검수 부하를 기준으로 빌드·론칭 신규 유입을 코치가 조절
- **1인 운영 리스크 대응:** 방법론 IP 문서화, (Phase 2) AI 초안 자동화로 검수 시간 단축, 정원 제한

---

## 10. SEO 및 메타데이터

- **[P1] 브랜드 정합성:** 메타 타이틀·OG의 구 브랜드("꿈몰다 브랜드 매니지먼트") 잔재를 **"한끗프로젝트"**로 통일
- 한국어(`lang="ko"`), Noto 폰트 preconnect, Netlify Forms 연동 [현행]

---

## 11. 구현 완성도 (현행 ↔ Phase 1 목표)

| 항목 | [현행] | [P1 목표] |
| :--- | :---: | :---: |
| 데이터 영속성 | localStorage(기기 종속) | **Supabase DB** |
| 어드민 인증 | 없음(Placeholder) | **Supabase Auth** |
| 멤버 비밀번호 | 평문 | **Auth 위임(해싱)** |
| 개인정보 동의·처리방침 | 없음 | **동의 기록 + /privacy** |
| 검수 게이트 | 없음 | **RLS 강제(finalized)** |
| 음성 입력 | 구현(localStorage) | **flag OFF**(텍스트 전용) |
| 이메일 전송 | 없음 | [P1.5] |
| AI 분석 | 로컬 룰 | [P2] LLM API |
| 결제 | 없음 | [P3] |

**완료(현행 유지):** 랜딩 10섹션, 서비스 소개, 무료 7문항 진단·리포트, 상담·4종 신청, 멤버 로그인, 42문항(텍스트), 대시보드·리뷰·제출, 어드민 2탭, 워크스페이스, 알림, 반응형

---

## 12. 향후 로드맵

| Phase | 범위 | 항목 |
| :--- | :--- | :--- |
| **Phase 1** (출시 전) | 인프라·보안·검수·동의 | Supabase 이전, Auth 인증, 비번 해싱, 검수 게이트, 동의·처리방침, 음성 OFF (작업 명세 T1~T10) |
| **Phase 1.5** (출시 직후) | 전환 누수 차단·음성 복원 | 진단 결과·상담 확인 **이메일 전송**, **음성 클라우드 저장(B2)** 후 음성 재활성화 |
| **Phase 2** | AI 연동(핵심 가치 강화) | `coachingAI` → LLM API(마스터 브리프·원라이너 자동 초안, 내부 도구로만), STT |
| **Phase 3** | 비즈니스 확장 | 결제(토스/카카오페이), PPT 자동 Export, GA4·Sentry, 변화 리포트 |

> AI는 외부에 브랜딩하지 않는다. AI 자동화는 코치 검수 생산성을 높이는 내부 도구로 위치한다.

---

## 13. 출시 기준 (Definition of Done) [P1]

- [ ] 전 테이블 RLS enable, 비로그인 리드·답변 조회 불가
- [ ] `/admin`·워크스페이스 비로그인 차단
- [ ] service_role 키가 클라이언트 번들에 미포함
- [ ] 멤버 비밀번호가 평문으로 어디에도 없음
- [ ] 폰↔PC 교차 로그인 시 42문항 이어쓰기 정상
- [ ] `VITE_VOICE_ENABLED=false`에서 녹음 UI 비노출
- [ ] finalized 전 리포트 비노출, 확정 후 노출
- [ ] 모든 수집 지점 동의 체크 + 시각 저장, 미동의 제출 차단
- [ ] `/privacy` 접근 가능
- [ ] Supabase 자동 백업 활성화

---

## 14. FAQ (고객 대상)

| Q | A |
| :--- | :--- |
| 아직 명확한 목표가 없어도 신청할 수 있나요? | 가능합니다. 50만 원의 한끗 진단으로 방향부터 함께 잡아드립니다. |
| 강사 양성 과정인가요? | 아닙니다. 이미 경력 있는 전문가의 콘텐츠를 함께 설계하는 서비스입니다. |
| 퇴직 전에도 받을 수 있나요? | 오히려 퇴직 6개월~1년 전에 시작하면 가장 효과적입니다. |
| 결과물은 어디에 활용할 수 있나요? | 강의 제안, 자문 계약, 컨설팅 수주, SNS 콘텐츠 등에 즉시 활용 가능합니다. |
| 결과가 보장되나요? | 산출물은 6주 안에 완성됩니다. 시장 반응은 개인 경력과 실행에 따라 다릅니다. |

---

> **문서 끝** | v3.0 (출시 기준 · Phase 1 baseline) | 부속 문서: 「한끗프로젝트 Phase 1 작업 명세」
