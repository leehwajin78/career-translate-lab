# 회원 관리 페이지 기능 개선 및 UI 세분화 구현 계획

회원 관리 화면("발급된 회원 관리")의 사용성을 개선하여, 회원들을 한끗 서비스의 4단계(한끗진단, 한끗빌드, 한끗론칭, 한끗파트너)에 따라 분류하여 조회할 수 있도록 하고, 관리 항목들을 드롭다운 및 인라인 입력 폼 형태로 변경하여 마우스 클릭과 키보드 입력만으로 손쉽게 수정 및 관리할 수 있도록 합니다.

## User Review Required

> [!IMPORTANT]
> - 회원 상세 항목(이름, 이메일, 비밀번호)은 테이블 셀 안에서 직접 텍스트 인라인 입력을 통해 즉각 수정 및 자동 저장(Zustand 스토어 및 LocalStorage 반영)되도록 구현합니다.
> - 가입 서비스(상품 패키지)와 코칭 진행 상태는 마우스 클릭으로 간편하게 조작할 수 있도록 `<Select>` 드롭다운 컴포넌트로 변경합니다.

## Open Questions

없음 (요구사항이 명확함)

## Proposed Changes

---

### [Zustand Auth Store]

#### [MODIFY] [authStore.ts](file:///e:/꿈몰다/외부사업/2026/모두의연구소/career-translate-lab/src/store/authStore.ts)
- `AuthState` 인터페이스에 회원 정보를 수정할 수 있는 `updateMember` 액션 정의를 추가합니다.
- `useAuthStore` 스토어 구현부에 `updateMember`를 작성하여 회원의 이름, 이메일, 비밀번호, 가입 상품(`productKey`) 등의 필드를 부분적으로 업데이트하고 상태를 반영할 수 있도록 합니다.

---

### [Admin Dashboard UI]

#### [MODIFY] [Admin.tsx](file:///e:/꿈몰다/외부사업/2026/모두의연구소/career-translate-lab/src/pages/Admin.tsx)
- 발급된 회원 관리 테이블 상단에 **회원 단계별 필터 필(Pill)**을 추가합니다:
  - `전체` (All)
  - `한끗 진단` (Diagnosis)
  - `한끗 빌드` (Build)
  - `한끗 론칭` (Launch)
  - `한끗 파트너` (Partner)
- 선택된 필터에 따라 회원 목록을 필터링하여 렌더링합니다.
- 발급된 회원 테이블 내의 관리 항목을 변경 가능한 컨트롤로 교체합니다:
  - **이름**: 입력 필드 (`<input type="text">` 스타일링 적용)
  - **이메일 ID**: 입력 필드 (`<input type="text">` 스타일링 적용)
  - **비밀번호**: 입력 필드 (`<input type="text">` 스타일링 적용)
  - **가입 서비스**: `<Select>` 컴포넌트로 변경하여 4개 상품군 선택 가능
  - **상태**: `<Select>` 컴포넌트로 변경하여 코칭 진행 상태(`SubmissionStatus`의 5단계) 선택 가능
- 정보 변경 시 toast 알림을 띄워 변경 사항이 성공적으로 저장되었음을 피드백합니다.

---

## Verification Plan

### Automated Tests
- `npm run test` 실행을 통해 기존 코드가 정상 동작하는지 검증합니다.
- `src/test/authStore.test.ts` 파일을 신설하여 `updateMember` 액션이 회원 데이터를 올바르게 갱신하는지 테스트 코드로 검증합니다.

### Manual Verification
1. 관리자 콘솔의 `코칭 회원 및 ID 관리` 탭으로 이동합니다.
2. 테이블 위에 새로 추가된 단계별(한끗진단, 한끗빌드, 한끗론칭, 한끗파트너) 필터 버튼들을 클릭하여 회원이 각 분류대로 필터링되는지 확인합니다.
3. 회원 테이블 내의 이름, 이메일 ID, 비밀번호 입력란을 임의로 수정한 뒤, 다른 페이지에 갔다 돌아오거나 새로고침하여 상태가 정상 유지(자동 저장)되는지 검증합니다.
4. 가입 서비스 및 상태 셀의 드롭다운을 클릭하여 변경하고, 변경된 내용이 회원 목록 및 필터링에 실시간으로 반영되는지 확인합니다.
