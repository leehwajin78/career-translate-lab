-- =============================================================
-- 005 — leads.memo 컬럼 추가 (관리자 인라인 메모 영속화, MVP DB 연동 Phase 1)
-- 안전: nullable TEXT 추가 (기존 데이터 영향 없음)
-- =============================================================

ALTER TABLE leads ADD COLUMN IF NOT EXISTS memo TEXT;
