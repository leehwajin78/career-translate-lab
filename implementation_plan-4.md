# 회원 관리 및 1:1 코칭 워크스페이스 개편 계획 (2차 수정)

1:1 코칭 워크스페이스(`CoachingWorkspace`)에서 현재 코칭 중인 회원의 신청 상품 단계(한끗진단, 한끗빌드, 한끗론칭, 한끗파트너)를 직관적으로 구분할 수 있도록 하고, 테스트나 코칭 과정 중 AI 분석 가이드 내용이 비어있을 때 이를 직접 해소할 수 있는 대화형 제어 도구를 구현합니다.

## User Review Required

> [!IMPORTANT]
> - **헤더 배지 추가**: 상단 헤더에 현재 코칭 중인 회원의 단계 배지(예: `한끗 진단`, `한끗 빌드`, `한끗 론칭`, `한끗 파트너`)를 컬러별로 명시합니다.
> - **다이내믹 테마 적용**: 회원의 단계에 맞추어 워크스페이스의 주요 포인트 컬러(최종 확정 버튼, 미니맵 활성 문항, 분석 아이콘 등)를 동적으로 변경합니다:
>   - **한끗 진단**: 파란색 (Blue) 테마
>   - **한끗 빌드**: 황금색 (Amber/Gold, #C4A265) 테마
>   - **한끗 론칭**: 장미색 (Rose) 테마
>   - **한끗 파트너**: 에메랄드색 (Emerald) 테마
> - **AI 가이드 부재 현상 제어**: 회원이 아직 진단 질문지를 작성하지 않았거나 AI 분석 데이터(`aiDraft`)가 생성되지 않아 가이드 영역이 비어있을 경우, 관리자가 즉시 테스트할 수 있도록 **"테스트용 샘플 답변 & AI 분석 생성"** 또는 **"AI 분석 실행 및 가이드 생성"** 버튼을 제공합니다.

## Open Questions

없음

## Proposed Changes

---

### [Coaching Workspace UI]

#### [MODIFY] [CoachingWorkspace.tsx](file:///e:/꿈몰다/외부사업/2026/모두의연구소/career-translate-lab/src/pages/coaching/CoachingWorkspace.tsx)
- `analyzeCoachingAnswers` 함수를 `src/lib/coachingAI.ts`로부터 임포트합니다.
- 각 상품별 배지 라벨 및 Tailwind CSS 테마 컬러 셋을 반환하는 헬퍼 함수 `getProductTheme`를 정의합니다.
- 헤더 영역의 타이틀 옆에 회원의 상품 배지를 렌더링합니다.
- 워크스페이스 내부 요소에 동적 컬러 테마 클래스를 적용합니다 (최종 확정 버튼, 미니맵 활성 문항, PART 배지, 아이콘 등).
- **AI 분석 & 1차 코칭 가이드** 가이드 영역 하단의 렌더링 조건부를 변경합니다:
  - `aiDraft` 데이터가 없을 때 경고 대신 인터랙티브 안내 창 표시.
  - 회원의 기존 답변이 있을 경우: **"AI 분석 실행 및 가이드 생성"** 핸들러 추가.
  - 회원의 기존 답변이 없을 경우: 테스트용 8개 대표문항 샘플 답변 세트를 채우고 분석 결과까지 생성해 주는 **"테스트용 샘플 답변 & AI 분석 생성"** 핸들러 추가.

---

## Verification Plan

### Automated Tests
- `npm run test`를 실행하여 컴파일 에러 및 기존 테스트 성공 여부를 확인합니다.

### Manual Verification
1. 관리자 콘솔에서 `코칭 회원 및 ID 관리`로 가서 새로 만든 테스트용 회원(`test@example.com` 등)의 `코칭 시작` 버튼을 클릭합니다.
2. 상단 헤더에 설정한 가입 서비스 배지가 잘 뜨는지, 버튼 등이 테마 색상으로 렌더링되는지 확인합니다.
3. 좌측 하단의 AI 가이드 영역에 "AI 분석 & 가이드 초안이 생성되어 있지 않습니다." 안내 창과 함께 **"테스트용 샘플 답변 & AI 분석 생성"** 버튼이 노출되는지 확인합니다.
4. 해당 버튼을 클릭하고 잠시 대기한 뒤, 페이지가 새로고침되며 8대 브랜드 프로필 빌더 및 문항별 AI 가이드 내용이 실시간으로 정상 바인딩되는지 확인합니다.
