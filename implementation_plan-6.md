# 1:1 코칭 워크스페이스 "진단 모드 / 빌드 모드" 토글 및 편집 잠금 구현 계획

진단(50만 원) 패키지와 빌드(350만 원) 패키지의 서비스 경계를 명확히 하고 운영 과투입을 방지하기 위해, 1:1 코칭 워크스페이스에 **"진단 모드 / 빌드 모드" 토글**을 추가하고 진단 모드 시 원라이너와 강점 이외의 6개 컴포넌트를 편집 잠금 처리합니다.

## User Review Required

> [!IMPORTANT]
> - **세션별 모드 저장**: 선택된 모드는 브라우저를 새로고침하거나 재접속하더라도 유지될 수 있도록 `localStorage`에 `workspace-mode:{sessionId}` 키로 저장됩니다.
> - **잠금 디자인 적용**: 진단 모드 시 잠금 대상인 6개 카드는 화면에서 완전히 사라지지 않고, 반투명(`opacity-60`) 처리 및 `disabled` 속성이 적용되며 우상단에 **자물쇠 아이콘 + "빌드 단계에서 작성됩니다"** 안내 배지가 추가되어 직관적으로 구분됩니다.
> - **초안 대조 버튼 비활성화**: 진단 모드에서 잠긴 6개 카드의 `🤖 초안대조` 및 `원복` 버튼도 비활성화되어 경계 침범을 원천 차단합니다.
> - **버튼 라벨 다이내믹 변경**: 우상단 및 우하단 전송/확정 버튼의 텍스트가 현재 선택된 모드에 맞게 동적으로 변경됩니다.

## Open Questions

없음 (요구사항이 명확히 제시됨)

## Proposed Changes

---

### [Coaching Workspace]

#### [MODIFY] [CoachingWorkspace.tsx](file:///e:/꿈몰다/외부사업/2026/모두의연구소/career-translate-lab/src/pages/coaching/CoachingWorkspace.tsx)
- `lucide-react`에서 `Lock` 아이콘을 추가로 임포트합니다.
- **모드 상태 선언 및 초기화**:
  - `mode` 상태(`"diagnosis" | "build"`)를 정의합니다.
  - 초기값은 `localStorage`에서 `workspace-mode:${memberId}` 키로 불러오며, 값이 없으면 기본값인 `"diagnosis"`로 설정합니다.
  - 모드 변경 시 해당 값을 `localStorage`에 즉시 반영하는 `handleModeChange` 함수를 작성합니다.
- **상단 헤더에 세그먼트 토글 추가**:
  - 회원 세션 배지 우측이자 임시 저장 버튼 좌측인 `flex items-center gap-3` 컨테이너의 첫 번째 요소로 세그먼트 토글 `[ 진단 모드 | 빌드 모드 ]`을 배치합니다.
  - 선택된 모드는 브랜드 네이비 배경(`bg-[#1E2D8C] text-white`), 선택되지 않은 모드는 라벤더 톤(`text-[#1E2D8C] hover:bg-slate-200/50`)을 적용합니다.
- **최종 확정 버튼 라벨 동적 처리**:
  - 헤더와 하단 액션바에 위치한 최종 확정 버튼들의 라벨을 다음과 같이 모드에 따라 다르게 표시합니다:
    - **진단 모드**: `{isFinalized ? "진단 결과 수정 및 적용" : "진단 결과 확정 및 전달"}`
    - **빌드 모드**: `{isFinalized ? "최종 브랜드 프로필 수정 및 적용" : "최종 브랜드 프로필 확정 및 전달"}`
- **안내문 동적 처리**:
  - 빌더 최상단 가이드 문구를 선택된 모드에 따라 변경합니다:
    - **진단 모드**: `"진단 단계 — 원라이너와 강점만 확정합니다. 나머지는 빌드에서 펼쳐집니다."`
    - **빌드 모드**: `"AI 초안을 디딤돌 삼아, 코치 인터뷰 대화를 반영한 최종 브랜드 결과물을 정교화합니다. 각 요소를 근거 질문과 매칭해 보완하세요."`
- **프로필 빌더 카드 제어 (핵심 가치, 타깃 페르소나, 브랜드 스토리, 핵심 메시지, 채널 전략, 브랜드 WHY)**:
  - 진단 모드일 때 해당 6개 카드 컨테이너에 `opacity-60 pointer-events-none` (혹은 input/button 개별 disabled) 스타일을 적용하여 흐리게 처리합니다.
  - 각 카드의 입력 폼(`input`, `textarea`)과 초안대조/되돌리기 버튼에 `disabled={mode === "diagnosis"}`를 설정합니다.
  - 카드 우상단 헤더 영역에 `mode === "diagnosis"`인 경우 `Lock` 아이콘과 함께 **"빌드 단계에서 작성됩니다"** 문구를 렌더링합니다.

---

## Verification Plan

### Automated Tests
- `npm run test`를 실행하여 컴파일 에러 및 기존 테스트가 정상 통과함을 보증합니다.

### Manual Verification
1. 로컬 1:1 코칭 워크스페이스([http://localhost:8080/coaching/workspace/member-id](http://localhost:8080/coaching/workspace/member-id))에 진입합니다.
2. 헤더에 세그먼트 토글이 추가되었는지, 기본값이 **진단 모드**로 선택되었는지 확인합니다.
3. 진단 모드 상태에서 우측 프로필 빌더의 "브랜드 원라이너"와 "강점 명제문"은 정상 수정이 가능하고, 나머지 6개 항목은 disabled 및 흐리게 표시되는지 확인합니다.
4. 잠긴 항목 우상단에 자물쇠 아이콘과 "빌드 단계에서 작성됩니다" 안내가 표시되는지 확인합니다.
5. "빌드 모드"로 전환한 뒤 8개 항목 모두 정상적으로 편집 및 초안대조가 가능해지는지 확인합니다.
6. 모드를 변경한 후 페이지를 새로고침했을 때, 선택했던 모드가 그대로 유지되는지 검증합니다.
7. 최종 전송 버튼의 라벨이 모드에 따라 적절하게 변화하는지 확인합니다.
